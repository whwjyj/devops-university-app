pipeline {
    agent any

    stages {
        stage('Maven Build') {
            steps {
                sh 'pwd'
                sh 'ls -al'
                sh 'mvn clean'
            }
        }
    }
}
